<Window x:Class="WPF_Mysteries.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Window2" Height="300" Width="300"
        Background="Black"
        Foreground="White"
        HorizontalContentAlignment="Stretch"
        VerticalContentAlignment="Stretch">
	<Window.Resources>
		<Style x:Key="ButtonStyle1" TargetType="{x:Type Button}">
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="White" />
								<Setter TargetName="border" Property="BorderBrush" Value="Black" />
								<Setter Property="Foreground" Value="Black" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="Black" />
								<Setter TargetName="border" Property="BorderBrush" Value="White" />
								<Setter Property="Foreground" Value="White" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
		<Style x:Key="ButtonStyle2" TargetType="{x:Type Button}">
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type Button}">
						<Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center">
								<ContentPresenter.Resources>
									<Style TargetType="TextBlock">
										<Setter Property="Foreground" Value="White" />
									</Style>
								</ContentPresenter.Resources>
							</ContentPresenter>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="border" Property="Background" Value="#FF5E5E5E" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="border" Property="Background" Value="#FF303030" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Border Grid.Row="0" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
			<TextBlock Text="TITLE" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
		</Border>
		<Border Grid.Row="1" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>
				<Border Grid.Row="0" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 1" Style="{StaticResource ButtonStyle1}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
				<Border Grid.Row="1" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 2" Style="{StaticResource ButtonStyle2}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
				<Border Grid.Row="2" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 3" Style="{StaticResource ButtonStyle1}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
				<Border Grid.Row="3" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 4" Style="{StaticResource ButtonStyle2}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
				<Border Grid.Row="4" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 5" Style="{StaticResource ButtonStyle1}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
				<Border Grid.Row="5" Grid.Column="0" Margin="10" Background="Transparent" BorderBrush="White" BorderThickness="1" CornerRadius="5">
					<TextBlock Text="Button 6" Style="{StaticResource ButtonStyle2}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,10,0" />
				</Border>
			</Grid>
		</Border>
	</Grid>
</Window>								